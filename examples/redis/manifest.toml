# simplest possible manifest — a single redis instance

[service.redis]
image = "redis:7"
ports = ["6379:6379"]
restart = "always"

[service.redis.health_check]
type = "tcp"
port = 6379
interval = 10
